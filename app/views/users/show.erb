<h3>Profile</h3>

<strong>Your subscribtions:</strong>
<% if @user.channels.any? %>
  <ul class="prof">
    <% @user.channels.each do |chan| %>
        <li><%= chan.name %></li>
        <form action="/users/<%=@user.id%>/<%=chan.id%>" method="post">
          <input type="hidden" name="_method" value="delete">
          <input type="submit" value="Unsubscribe">
        </form>
    <% end %>
  </ul>
    <p>Cost of your package:</p>
    <strong><%=@user.channels.map(&:price_per_month).reduce("+")%> </strong>
<% else %>
    <p>You don't have any!</p>
<% end %>

<p><a href="/channels">Main</a> </p>

